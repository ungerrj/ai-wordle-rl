__pycache__/
*.pyc
.ipynb_checkpoints/

# Training output (the directory must exist, so keep the placeholder)
results/*
!results/.gitkeep

# Downloaded word lists (environment/word_lists.py caches them here). Keep the
# placeholder so the directory exists for the compose bind mount.
data/*
!data/.gitkeep
